# unified-ai-security
# Python 3.10+
#
# Install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#

# Core ML
sentence-transformers>=2.2.0
numpy>=1.24,<3

# RAG vector store
chromadb>=0.4.22,<2

# Schemas & validation
pydantic>=2.0

# Env / config
python-dotenv>=1.0
PyYAML>=6.0

# API gateway
fastapi>=0.100
uvicorn[standard]>=0.22
requests>=2.28
httpx>=0.24

# Tool execution (Hafta 14):
# simpleeval is an AST-restricted expression evaluator — never eval()/exec().
# `tools/calculator.py` falls back to a hand-rolled walker if absent, but
# we ship simpleeval explicitly so demo runs use the maintained library.
simpleeval>=0.9

# Evaluation report generation (Hafta 15):
# matplotlib used by scripts/generate_thesis_report.py to render the
# charts that ship as PNG attachments next to the markdown / PDF report.
# Headless Agg backend so it works in Docker without an X server.
matplotlib>=3.5

# Sandbox execution (Docker SDK)
docker>=7.0

# Dashboard UI (Streamlit, separate process from the gateway)
streamlit>=1.30
streamlit-autorefresh>=1.0
pandas>=2.0
plotly>=5.18
# PDF export from the Reports page. reportlab is the lightweight fallback;
# weasyprint+markdown gives prettier output if installed.
reportlab>=4.0

# Testing
pytest>=7.4
