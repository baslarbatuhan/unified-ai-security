# unified-ai-security
# Python 3.10+ (local venv); Docker image uses Python 3.11
#
# Local install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#   playwright install chromium    # only for external_eval web targets
#
# Docker: dependencies installed in Dockerfile; Chromium via `playwright install --with-deps`

# Core ML
sentence-transformers>=2.2.0
numpy>=1.24,<3

# RAG vector store (Chroma client; compose also runs chromadb/chroma server)
chromadb>=0.4.22,<2

# Schemas & validation
pydantic>=2.0

# Env / config
python-dotenv>=1.0
PyYAML>=6.0

# API gateway
fastapi>=0.100
uvicorn[standard]>=0.22
requests>=2.28
httpx>=0.24

# Local tool execution (tools_local adapter + Hafta 14 demos)
simpleeval>=0.9

# Web target adapter (Sprint 8): Playwright + Chromium for type=web targets.
# Browser binary: Dockerfile runs `playwright install --with-deps chromium`.
playwright>=1.40

# Evaluation / thesis report charts
matplotlib>=3.5

# Optional sandbox profile (docker-compose sandbox service)
docker>=7.0

# Dashboard (Streamlit — separate process from gateway)
streamlit>=1.30
streamlit-autorefresh>=1.0
pandas>=2.0
plotly>=5.18
reportlab>=4.0

# Test suite (~609 tests)
pytest>=7.4
