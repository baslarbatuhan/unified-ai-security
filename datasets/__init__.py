"""Curated attack/benign datasets for regression checks.

`prompt_regression_set.json` is the small pinned set used by
`evaluation/prompt_guard_stability.py --dataset`. See `dataset_loaders.py`
for the programmatic loader + metric helpers.
"""
